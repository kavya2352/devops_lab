apiVersion: v1
kind: Service
metadata:
  name: quiz-app-service
spec:
  selector:
    app: quiz-app
  type: NodePort     # ✅ For local minikube or on-prem deployment
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080   # Access via Node IP:30080